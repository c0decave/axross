"""axross security-policy package.

Modules here define cross-cutting safety caps and validation helpers
that other layers (e.g. ``core/``) import to enforce defense-in-depth
boundaries independent of the call-site logic.
"""
