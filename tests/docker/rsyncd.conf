uid = rsyncuser
gid = rsyncuser
use chroot = no
max connections = 4
log file = /dev/stdout

[testmod]
    path = /srv/rsync/testmod
    comment = Test module
    read only = no
    auth users = rsyncuser
    secrets file = /etc/rsyncd.secrets
